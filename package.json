{
  "name": "nfc-pay-vercel",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "vercel-build": "( if [ -n \"$DATABASE_URL\" ] && [[ \"$DATABASE_URL\" == *\"-pooler\"* ]] && [[ \"$DATABASE_URL\" == *\"sslmode=require\"* ]]; then npx prisma migrate deploy || npx prisma db push; else echo 'DATABASE_URL not set or not pooled; skipping Prisma'; fi ) && next build",
    "postinstall": "prisma generate",
    "start": "next start -p $PORT"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "next": "14.2.5",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "prisma": "5.22.0",
    "@types/node": "^20.11.30",
    "@types/react": "^18.2.66",
    "typescript": "^5.4.5"
  }
}